The Mocha test cases can be run as usual on linux 
but on windows, mocha-runner needs to be used which accepts the run file which has the test case file list.

On windows it can be run using Windows PowerShell, just open PowerShell and cd to the location /test

and say node run